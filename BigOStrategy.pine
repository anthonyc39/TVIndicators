// Updated buy conditions to align with green triangle
strategy.entry("Buy", strategy.long, when = (condition_for_green_triangle and close == 25313.00))

// Take Profit and Stop Loss settings
strategy.exit("Take Profit/Stop Loss", from_entry="Buy", limit=25334.50, stop=25287.00)

// Diagnostic plots for validation
plot(indicator_name, color=color.green, title="Diagnostic Indicator")
plotshape(series=condition_for_green_triangle, location=location.belowbar, color=color.green, style=shape.triangleup, title="Buy Signal")
